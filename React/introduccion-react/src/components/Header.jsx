// eslint-disable-next-line react/prop-types
function Header ({ titulo, subtitulo }) {
  return(
    <header>
      <h1>{titulo}</h1>
      <p>{subtitulo}</p>
    </header>
  )
}

export default Header

